apiVersion: v1
kind: Pod
metadata:
  name: chart
  labels:
    name: chart
spec:
  containers:
  - name: chart
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 5155
